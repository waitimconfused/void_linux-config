{
	match = " class_i = 'i3lock' ";

	animations = (
		
		# In
		{
			triggers = [ "open", "show" ];

			preset = "slide-in";
			direction = "up";
		},

		# Out
		{
			triggers = [ "close", "hide" ];
			preset = "slide-out";
			direction = "up";
			duration = 0.2;
		}
	);
}
