{
	match = "fullscreen";
	shadow = false;
	corner-radius = 0;

	animations = (
		
		# In
		{
			triggers = [ "show" ];
			preset = "appear";
			scale = 1.1;
			duration = 0.1;
		},

		# Out
		{
			triggers = [ "hide" ];
			preset = "disappear";
			scale = 1.1;
			duration = 0.1;
		}
	);
}
